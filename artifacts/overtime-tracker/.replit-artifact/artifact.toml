kind = "web"
previewPath = "/"
title = "Overtime Tracker"
version = "1.0.0"
id = "artifacts/overtime-tracker"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25318

[services.development]
run = "pnpm --filter @workspace/overtime-tracker run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/overtime-tracker", "run", "build" ]
publicDir = "artifacts/overtime-tracker/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25318"
BASE_PATH = "/"
